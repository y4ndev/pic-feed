'use client';

import Header from '@/components/Header/Header';

const Feed = () => {
  return (
    <div className='feed'>
      <Header showProfile title={'Pic-feed'} />
    </div>
  );
};

export default Feed;
